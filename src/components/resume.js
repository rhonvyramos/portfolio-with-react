import React from "react";

export default function ResumePage({selectedPage, setPage}) {
    console.log("Selected page is: " + selectedPage);
    return (
        <h1>Placeholder Resume Header</h1>
    )
}