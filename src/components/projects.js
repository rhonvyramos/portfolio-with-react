import React from "react";

export default function ProjectPage({selectedPage, setPage}) {
    console.log("Selected page is: " + selectedPage);
    return (
        <h1>Placeholder Project Header</h1>
    )
}