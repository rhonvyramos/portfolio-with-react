import React from "react";

export default function ContactPage({selectedPage, setPage}) {
    console.log("Selected page is: " + selectedPage);
    return (
        <h1>Placeholder Contact Header</h1>
    )
}